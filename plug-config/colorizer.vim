" Set maximum lines to color
let g:colorizer_maxlines=1000
" Don't enable on start up
let g:colorizer_startup=0
" Don't make default mappings
let g:colorizer_nomap=1

" Mapping is set in plug-config/WhichKey.vim
